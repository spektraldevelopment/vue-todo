<template>
    <div class="todo-list">
        <ul>
            <!-- <li v-for="item in getToDoItems" :key="item.id">
                <span>{{ item.name }}</span>
                <button @click="onItemComplete(item)">Done</button>
            </li> -->
        </ul>
    </div>
</template>

<script>
export default {
    name: "Completed"
}
</script>
